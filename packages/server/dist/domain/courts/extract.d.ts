export declare class Extract {
    name: string;
    display: string;
    isPatient: boolean;
    rank: number;
    _id: string;
    constructor(name: string, display: string, isPatient: boolean, rank: number);
    toString(): string;
}
