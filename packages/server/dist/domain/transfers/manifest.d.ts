export declare class Manifest {
    mId: string;
    logDate: Date;
    buildDate: Date;
    docket: string;
    patientCount: number;
    cargo: any;
    _id: string;
    constructor(mId: string, logDate: Date, buildDate: Date, docket: string, patientCount: number, cargo: any);
}
